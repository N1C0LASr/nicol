#Referencia
#Obtener la determinante de una matriz de 3 x 3 con sus elementos aleatorios. 
#Se puede utilizar librerıas para resolver el algoritmo.


import numpy as np

matriz = np.random.rand(3, 3)  # crear matriz de números aleatorios
determinante = np.linalg.det(matriz)  # calcular determinante
print(matriz)
import numpy as np

matriz = np.random.rand(3, 3)  # crear matriz de números aleatorios
determinante = np.linalg.det(matriz)  # calcular determinante
print(matriz)
print ("El determinante de la matriz es: "), determinante

#fin